{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^4.0.0"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "forcePublish": "lerna publish --force-publish --no-git-tag-version --no-push"
  }
}
